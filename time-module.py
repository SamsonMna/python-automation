import time
def calProd():
    # Calculate the product of the first 100,000 numbers
    product = 1
    for i in range(1100000):
        product = product* i
    return product
    
startTime = time.time()
prod = calcprod()
endTime = time.time()
print("The result is %s digits long." % (len(str(prod))))
print("Took %s seconds to calculate." % (endTime - startTime))
# the sleep time
for i in range(x): # X being the number of interval for sleep
    print('Tick')
    time.sleep(1)
    print('Tock')
    time.sleep(1)
    print('Tick')
    time.sleep(n) # n of range(x) above
    
    
# Rounding time numbers
now = time.time()
now
round(now, 2) # Round the current time to the nearest two decimal points

  
    
